{
    "license": "Apache-2.0",
    "name": "www",
    "version": "5.0.0",
    "description": "hideo54's Homepage",
    "homepage": "https://hideo54.com",
    "private": true,
    "scripts": {
        "build": "pug src/[^_]*.pug -O data.json --out dist/ && cp -R pub/ dist/pub/",
        "ampbuild": "pug ampsrc/[^_]*.pug -O data.json --out ampdist/ && cp -R pub/ ampdist/pub/",
        "ampcheck": "amphtml-validator amp/*.html"
    },
    "repository": {
        "type": "git",
        "url": "git@github.com:hideo54/www.git"
    },
    "dependencies": {
        "pug-cli": "^1.0.0-alpha6"
    },
    "devDependencies": {
        "amphtml-validator": "^1.0.15"
    }
}

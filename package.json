{
    "license": "Apache-2.0",
    "name": "homepage",
    "version": "6.0.0",
    "description": "hideo54's Homepage",
    "homepage": "https://hideo54.com",
    "private": true,
    "scripts": {
        "build": "pug src/ -O data.yaml -o dist/ && lessc --clean-css src/style.less dist/style.css",
        "deploy": "git checkout gh-pages && cp dist/* ./; git add --all; git commit -m 'Update'; git push origin gh-pages; git checkout master"
    },
    "repository": {
        "type": "git",
        "url": "git@github.com:hideo54/homepage.git"
    },
    "dependencies": {
        "less": "^3.11.0",
        "less-plugin-clean-css": "^1.5.1",
        "pug-cli": "git+https://github.com/hideo54/pug-cli#master"
    }
}

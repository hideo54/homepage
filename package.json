{
    "license": "Apache-2.0",
    "name": "www",
    "version": "5.2",
    "description": "hideo54's Homepage",
    "homepage": "https://hideo54.com",
    "private": true,
    "scripts": {
        "build": "pug src/[^_]*.pug -O data.json --out dist/ && cp src/style.css dist/ && cp src/circle_color.css dist/ && cp src/icon.png dist/ && cp -R src/favicon/ dist/favicon/"
    },
    "repository": {
        "type": "git",
        "url": "git@github.com:hideo54/www.git"
    },
    "dependencies": {
        "pug-cli": "^1.0.0-alpha6"
    }
}

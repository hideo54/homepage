{
    "license": "Apache-2.0",
    "name": "www",
    "version": "5.0.0",
    "description": "hideo54's Homepage",
    "homepage": "https://hideo54.com",
    "private": true,
    "scripts": {
        "build": "pug src/ -O src/data.json --out web/ && rm web/_layout.html && echo \"deleted _layout.html\" && cp -R pub/ web/pub/",
        "ampbuild": "pug ampsrc/ -O src/data.json --out amp/ && rm amp/_layout.html && echo \"deleted _layout.html\" && cp -R pub/ amp/pub/",
        "ampcheck": "amphtml-validator amp/*.html"
    },
    "repository": {
        "type": "git",
        "url": "git@github.com:hideo54/www.git"
    },
    "dependencies": {
        "pug-cli": "^1.0.0-alpha6"
    },
    "devDependencies": {
        "amphtml-validator": "^1.0.15"
    }
}
